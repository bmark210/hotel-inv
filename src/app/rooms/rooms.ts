export interface RoomList {
  roomNumber: string;
  roomType: string;
  amenities: string;
  price: number;
  photos: string;
  checkinTime: string;
  checkoutTime: string;
  rating: number;
  selected?: boolean;
}
